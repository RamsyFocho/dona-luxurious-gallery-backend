{
  "version": 2,
  "buildCommand": "npm run build && npm run prisma:deploy",
  "builds": [
    {
      "src": "api/index.ts", 
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
