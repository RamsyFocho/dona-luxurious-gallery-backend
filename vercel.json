{
  "version": 2,
  "buildCommand": "npm run build && npm run prisma:generate && npx prisma migrate deploy --schema=prisma/schema.prisma && npx ts-node prisma/seed.ts",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/health",
      "dest": "api/health.ts"
    },
    {
      "src": "/api/(.*)",
      "dest": "api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
