{
  "version": 2,
  "buildCommand": "npm run build && npm run prisma:generate && npm run prisma:deploy && npm run prisma:seed",
  "builds": [
    {
      "src": "api/index.ts", 
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
