{
  "version": 2,
  "buildCommand": "npm run build && echo '===> running prisma generate (postinstall also runs it)' && npm run prisma:generate && echo '===> running prisma migrate deploy' && npx prisma migrate deploy --schema=prisma/schema.prisma && echo '===> running prisma seed' && npx ts-node prisma/seed.ts",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/health",
      "dest": "api/health.ts"
    },
    {
      "src": "/api/(.*)",
      "dest": "api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
